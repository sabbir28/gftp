name: Auto Build & Release

on:
  push:
    branches:
      - main
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  build-and-release:
    runs-on: windows-latest
    permissions:
      contents: write

    defaults:
      run:
        shell: msys2 {0}

    steps:
      - name: Checkout Source Code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up MSYS2 MinGW-w64 GCC Toolchain
        uses: msys2/setup-msys2@v2
        with:
          msystem: MINGW64
          update: true
          install: >-
            mingw-w64-x86_64-gcc
            mingw-w64-x86_64-make

      - name: Generate Build Version Number
        id: versioning
        shell: powershell
        run: |
          $runNumber = "${{ github.run_number }}"
          $buildVer = "1.0.${runNumber}"
          $tagName = "v${buildVer}"
          echo "BUILD_VER=${buildVer}" >> $env:GITHUB_ENV
          echo "TAG_NAME=${tagName}" >> $env:GITHUB_ENV
          echo "Build Version: ${buildVer}"

      - name: Compile gftp.exe with Dynamic Build Number
        run: |
          g++ -O3 -std=c++17 -Wall -I. -DGFTP_BUILD_NUMBER="\"v${BUILD_VER}\"" src/*.cpp -o gftp.exe -lwininet -lshlwapi

      - name: Generate Automated Commit Changelog
        shell: powershell
        run: |
          echo "# gftp Commit Changelog History" > CHANGELOG.md
          echo "Generated automatically for build ${env:TAG_NAME} on $(Get-Date -Format 'yyyy-MM-dd HH:mm:ss')" >> CHANGELOG.md
          echo "" >> CHANGELOG.md
          git log --pretty=format:"* %s (%h - %an, %ad)" --date=short >> CHANGELOG.md

      - name: Verify Binary Version Output
        run: |
          ./gftp.exe version

      - name: Package Release ZIP Archive (Executable, README & Changelog ONLY)
        shell: powershell
        run: |
          $zipName = "gftp-${env:TAG_NAME}-windows-x64.zip"
          echo "ZIP_NAME=${zipName}" >> $env:GITHUB_ENV
          Compress-Archive -Path gftp.exe, README.md, CHANGELOG.md -DestinationPath $zipName

      - name: Create Git Tag and Publish GitHub Release
        shell: powershell
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          $tagName = "${env:TAG_NAME}"
          $zipName = "${env:ZIP_NAME}"
          
          # Configure Git user & update tag
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git tag $tagName -f
          git push origin $tagName -f

          $releaseNotes = @"
          # gftp Release ${tagName}
          
          Automated build release for **gftp** (Git-Aware FTP Differential Sync CLI for Windows).
          
          ### Package Contents:
          - `gftp.exe` (Standalone Windows C++ binary)
          - `README.md` (CLI Usage Guide & Manual)
          - `CHANGELOG.md` (Complete Git Commit History)
          
          ### Key Features:
          - Direct Remote Git Cloning & Auto Push (`gftp --git <url> --ftp <dir>`).
          - Remote FTP Directory Wipe Engine (`gftp clean-remote`).
          - Fast differential sync using Git commit history & SHA tracking (`.gftp_state`).
          - Native Windows WinINet API execution with 0 external DLL dependencies.
          
          ### Download Binary Package:
          Attached: `${zipName}`
          "@

          gh release create "$tagName" "$zipName" --target main --title "gftp $tagName" --notes "$releaseNotes"
